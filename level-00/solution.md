# Bandit Level 0 → 1

## Commands

```bash
ls
cat readme
```

## Why this works

The level 0 account starts in its home directory, where `readme` contains the next password. `cat` prints the file contents.
