# Bandit Level 3 → 4

## Commands

```bash
cd ~/inhere
ls -la
cat ./...Hiding-From-You
```

## Why this works

The target is a hidden file. `ls -la` reveals hidden entries, and `./` safely references the filename.
