# Bandit Level 14 → 15

## Commands

```bash
nc localhost 30000
```

Enter the current level's password when prompted.

## Why this works

A local network service on port 30000 validates the submitted password and returns the next level's password.
